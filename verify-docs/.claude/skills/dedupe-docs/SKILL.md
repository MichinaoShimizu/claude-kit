---
name: dedupe-docs
description: >
  複数の文書に同じ内容が言い換えて（パラフレーズで）重複していないかを、
  意味で判断して見つけ出し、移動とポインタ化のみで解消する際に参照する。
  verify-docs の重複検査（scripts/verify-docs.mjs）は段落単位の文字列一致
  （準一致検査も句読点・語尾の機械的な正規化のみ）しか見ないため、言い回しが
  異なる言い換えの重複は検知できない——この隙間を人・エージェントの判断で
  埋める。「重複を探してほしい」「正本性を上げたい」「言い換えで同じ内容が
  複数箇所に書かれていないか確認して」「これREADMEとSKILL.mdで同じこと
  書いてない？」「意味的な重複を直して」が該当する。文字列一致では
  見つからない類の重複を疑う場面では、明示的に依頼されていなくても必ず
  本スキルを参照すること。
---

# dedupe-docs —— 言い換えによる重複を意味で見つけて解消する

verify-docs の重複検査は「段落がそのまま一致するか」しか見ない。コピペした
重複は防げるが、同じ内容を自分の言葉で書き直した重複は素通りする。本スキルは
この隙間を、チェッカーではなく人・エージェントの判断で埋める。

## 前提

対象リポジトリに verify-docs 一式（`scripts/verify-docs.mjs` と
`.claude/skills/verify-docs/`）が導入済みであること。検査対象・設定・
規約（`allow-duplicate` マーカーなど）は流用する。verify-docs 自体の規約は
[verify-docs/SKILL.md](../verify-docs/SKILL.md) を参照する。

## 手順

### 1. 機械的重複の解消

```bash
node scripts/verify-docs.mjs
```

`[重複]`・`[準一致重複]` が出たら、まず
[verify-docs/SKILL.md](../verify-docs/SKILL.md)「3. 重複特定」の
手順で解消する。意味的な重複の判定は、機械的な重複が無い状態を前提に行う
（残ったままだと、どちらが本来の重複源か判断しづらい）。

### 2. 意味的重複の探索

対象は verify-docs が検査する文書一式と同じ（`excludePaths` で除外した
ディレクトリを除くリポジトリ全体の `*.md`。詳細:
[references/config.md](../verify-docs/references/config.md)
「検査対象の集め方」）。文字列としては一致しなくても、同じ主張・同じ手順・
同じ判断基準を説明している段落が複数の文書に無いかを判断する。

作業開始時に、対象文書の一覧をチェックリストとして先に作る。作り方は
[verify-docs/references/checklist.md](../verify-docs/references/checklist.md)
を参照する。全項目にチェックが入るまで「3. 確信度別の対応」には
進まない。

よくあるパターン:

- 入口文書（README・CLAUDE.md など）の一文を、詳細文書（docs・SKILL.md
  など）が言い換えて再度説明している
- 詳細文書へのポインタを置いた直後に、そのポインタ先の内容を要約して
  重ねて書いている（「詳細は X を参照」と言いながら X の内容を先に
  書いてしまっている）
- 複数の詳細文書が、同じ前提・同じ手順を独立に説明している
- Mermaid などのダイアグラムと地の文が、同じ関係・同じフローを別表現で
  重ねて説明している

### 3. 確信度別の対応

- **確信できる場合**（同じ主張を別の言葉で書いているだけと判断できる）→
  完全一致の重複と同じ手順で是正する。本来の置き場所（より詳細・下位の
  文書）を決め、他方はポインタ1行に置換する。移動とポインタ化のみとし、
  要約・言い換えによる内容変更は行わない
  （[verify-docs/SKILL.md「守ること」](../verify-docs/SKILL.md#守ること)）
- **確信が持てない場合**（意味が重なるのは一部だけ、対象読者・文脈が
  異なる、片方が要約でもう片方が手順の詳細、など）→ その場で断定して
  統合せず、検出内容と根拠を人に報告して判断を仰ぐ
- **意図して重複させるべき場合**（免責文言・定型の注意書きなど）→
  [config.md「意図した重複の許可」](../verify-docs/references/config.md#意図した重複の許可)
  の `allow-duplicate` マーカーを使う。統合の対象からも外す

本手順の判定はチェッカーと異なり再現性を持たない（同じ入力でも見落とし・
過検知が起こりうる）。誤った統合を単独で確定させない。

### 4. 是正後再検査

```bash
node scripts/verify-docs.mjs
```

ポインタ化に伴うリンク切れ・断片リンク切れ・サイズ超過が発生していないか
確認し、本スキルの是正がverify-docsの検査を新たに壊していないか担保する。

## 完了条件

以下を全て満たした時点で完了とする。

- チェックリストの全項目にチェックが入っている（未確認のまま残っている
  文書が無い）
- 確信できた重複は是正済み、確信が持てなかったものは検出内容・根拠と
  ともに人へ報告済みである
- 「4. 是正後再検査」の `node scripts/verify-docs.mjs` が
  「文書構造: すべて通過」で終わっている

## 対象外とする範囲

- **重複の判定を自動化・機械化すること。** LLM 判定は再現性を持たないため、
  CI の pass/fail には使わない（verify-docs のチェッカーには組み込まない）
- **文章の品質・正確性の判断。** verify-docs 本体と同じく、判定するのは
  「同じ内容が重複しているか」のみであり、内容の正否・簡潔さは対象外
- **意味的な類似度のしきい値の数値化。** 「何%似ていたら重複とみなすか」
  のような機械的な基準は設けない。判断そのものを人・エージェントに委ねる
